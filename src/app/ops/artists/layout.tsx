'use client';

import UnifiedShell from '@/components/ops/UnifiedShell';

export default function ArtistsOpsLayout({ children }: { children: React.ReactNode }) {
  return <UnifiedShell>{children}</UnifiedShell>;
}
